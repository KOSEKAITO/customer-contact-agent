# セットアップ手順書

## 前提条件
- Node.js インストール済み
- Python 3.8以上 インストール済み

---

## 手順1：プロジェクトフォルダを配置

ダウンロードした `customer-contact-agent` フォルダをPCの好きな場所に置いてください。
フォルダ内に以下のファイルがあることを確認：

```
customer-contact-agent/
├── CLAUDE.md          ← Claude Codeが読み込むプロジェクト説明書
├── triggers.json      ← 18個のトリガー定義
├── templates.json     ← 15個のテンプレート文面
├── app.py             ← Streamlit Webアプリのメインファイル
├── requirements.txt   ← Python依存パッケージ
└── SETUP.md           ← この手順書
```

---

## 手順2：Pythonパッケージのインストール

ターミナルでプロジェクトフォルダに移動し、以下を実行：

```bash
cd customer-contact-agent
pip install -r requirements.txt
```

---

## 手順3：アプリの起動（動作確認）

```bash
streamlit run app.py
```

ブラウザが自動で開き、「顧客連絡AIエージェント」の画面が表示されます。
（通常 http://localhost:8501 で開きます）

---

## 手順4：動作テスト

### テスト①：能動的連絡
1. 左サイドバーで「🔵 能動的連絡」を選択（デフォルト）
2. 「商談前」タブ → 「#1 商談予約確認」ボタンを押す
3. 顧客名に「テスト太郎」と入力
4. その他の差込情報（予約日時、参加URL等）を適当に入力
5. 「📝 ドラフト生成」ボタンを押す
6. メール文面がプレビューされることを確認
7. 文面を修正できることを確認
8. 「✅ 許可（送信）」ボタンを押す
9. 「送信完了」のメッセージが出ることを確認

### テスト②：受動的連絡
1. 左サイドバーで「🟢 受動的連絡」を選択
2. 顧客名に「テスト太郎」と入力
3. 受信メール本文に以下を貼り付け：
   「書類はいつ届きますか？まだ届いていないのですが。」
4. 「🔍 解析してドラフト生成」ボタンを押す
5. 自動でA-12（書類催促）テンプレートが選択されることを確認

---

## 手順5：Claude Codeでの改善・拡張

Claude Codeをこのプロジェクトフォルダで起動すると、
CLAUDE.mdを自動で読み込み、プロジェクトの全体像を理解した状態で作業できます。

```bash
cd customer-contact-agent
claude
```

Claude Codeに依頼できる改善例：

- 「テンプレートA-20の文面を修正したい。〜に変更してください」
- 「新しいトリガーを追加したい。契約書署名完了後のお礼メールを追加して」
- 「Gmail APIを接続して実際にメール送信できるようにして」
- 「送信ログをGoogleスプレッドシートに記録するようにして」
- 「LINE Messaging APIを接続して、LINEでも送信できるようにして」

---

## 今後のロードマップ

### レベル1（現在）
✅ トリガー選択 → ドラフト生成 → 許可ボタンで送信

### レベル2（次のステップ）
- メール送信と同時に顧客管理シートへ自動記録
- Gmail API / LINE Messaging API の接続

### レベル3（将来）
- CRMステータス変更の自動検知 → トリガー自動発火
- 日時ベースのリマインド自動送信
- 送信効果の自動分析（返信率等）
